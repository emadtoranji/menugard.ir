import {
  StoreCategoriesKey,
  StoreItemsCategoriesKey,
  StoreExplorerSource,
  ValidCurrency,
} from '../generated/client';

export const storeCategories = Object.values(StoreCategoriesKey);
export const storeCurrencies = Object.values(ValidCurrency);
export const storeItemsCategoriesKey = Object.values(StoreItemsCategoriesKey);
export const storeExplorerSource = Object.values(StoreExplorerSource);
