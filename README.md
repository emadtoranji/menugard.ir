# 🚀 پلتفرم منو دیجیتال | Digital Menu Platform

به پروژه **پلتفرم منو دیجیتال** خوش آمدید! یک راهکار جامع و متن‌باز (**Open Source**) برای مدیریت و نمایش منوی رستوران‌ها، کافه‌ها و فست‌فودها. این پلتفرم به صاحبان کسب‌وکارهای غذایی امکان ایجاد فروشگاه آنلاین را می‌دهد و مشتریان می‌توانند به راحتی منو را مشاهده و سفارش خود را ثبت کنند.

---

## 🛠 تکنولوژی‌های استفاده شده | Tech Stack

پروژه با جدیدترین تکنولوژی‌های توسعه وب ساخته شده است:

- **فریم‌ورک:** [Next.js 19](https://nextjs.org/) (با قابلیت‌های React)
- **استایل‌دهی:** [Tailwind CSS +4](https://tailwindcss.com/) و CSS
- **زبان:** TypeScript / JavaScript / JSX
- **پایگاه داده:** PostgreSQL
- **سیستم احراز هویت:** [Next-Auth v5](https://authjs.dev/)

---

## ✨ ویژگی‌های کلیدی | Key Features

### 👤 **پنل مدیریت صاحبان فروشگاه**

- **🔐 ثبت‌نام و ورود امن:** با پشتیبانی `next-auth-v5`.
- **🏪 ایجاد و مدیریت فروشگاه:** امکان ساخت فروشگاه جدید با جزئیات کامل.
- **📝 ویرایش اطلاعات عمومی:** تغییر نام، آدرس، توضیحات و سایر اطلاعات فروشگاه.
- **🍔 مدیریت کامل محصولات:** افزودن، ویرایش و حذف آیتم‌های منو (شامل تصویر، قیمت، توضیحات و ...).
- **🪑 مدیریت میزها:** تعریف و مدیریت شماره میزها برای فرآیند سفارش‌گیری.
- **⏰ تنظیم ساعت کاری:** مشخص کردن روزها و ساعات فعال بودن فروشگاه.
- **🗑️ کنترل کامل:** تمامی بخش‌ها قابلیت **ویرایش** و **حذف** دارند.

### 🍽 **سمت مشتریان و بازدیدکنندگان**

- **📱 مشاهده منوی اختصاصی:** دسترسی به صفحه عمومی و منوی هر رستوران یا کافه.
- **🗒️ سیستم یادداشت سفارش (سبد خرید):**
  - مشتریان می‌توانند محصولات مورد نظر را به یک لیست "یادداشت" اضافه کنند.
  - این لیست برای نمایش به گارسون (Waiter) و تسهیل ثبت سفارش نهایی طراحی شده است.
  - **🍪 ذخیره‌سازی هوشمند با Context API:** اطلاعات لیست یادداشت (سفارشات) با استفاده از React Context API مدیریت می‌شود و به همراه Cookies در مرورگر کاربر ذخیره می‌شود. پس از چند ساعت به صورت خودکار منقضی و حذف می‌گردد. (اگر مشتری روز بعد بازگردد، لیست جدید و خالی خواهد بود).

### 👮 **پنل مدیریت اصلی (Super Admin)**

- **👥 مدیریت کاربران پلتفرم:** مشاهده لیست تمامی کاربران ثبت‌نام شده (دسترسی ویژه برای مدیر کل سیستم).

### 💰 **سیستم کیف پول - _قابلیت آینده_**

- **💳 رابط واریز اعتبار:** این بخش به منظور افزایش موجودی و شارژ کیف پول طراحی شده است.
- **🚧 وضعیت فعلی:** این ماژول در حال حاضر **غیرفعال** است و صرفاً برای توسعه‌های آینده و آماده‌سازی بستر (**Future-proofing**) ایجاد شده است.

---

## 🚀 راهنمای نصب و اجرا | Getting Started

برای اجرای پروژه به صورت محلی، مراحل زیر را دنبال کنید:

1.  **کلون کردن مخزن:**

    ```bash
    git clone https://github.com/emadtoranji/menugard.ir.git
    cd menugard.ir
    ```

2.  **نصب وابستگی‌ها:**

    ```bash
    npm install
    # یا
    yarn install
    # یا
    pnpm install
    ```

3.  **تنظیمات محیطی (Environment Variables):**
    - فایل `.env.sample` را به `.env.local` کپی کنید.
    - متغیرهای داخل آن را با مقادیر مناسب (مانند آدرس دیتابیس، کلیدهای احراز هویت و ...) پر کنید.

4.  **اجرای پروژه در محیط توسعه:**

    ```bash
    npm run dev
    # یا
    yarn dev
    # یا
    pnpm dev
    ```

    - اپلیکیشن به طور پیش‌فرض در آدرس [http://localhost:4501](http://localhost:4501) در دسترس خواهد بود (برای تغییر پورت میتوانید فایل package.json را ویرایش کنید.)

---

## 🤝 مشارکت | Contribution

این پروژه کاملاً **متن‌باز (Open Source)** است! 🎉

**استفاده، کپی‌برداری، تغییر و توسعه** آن برای اهداف شخصی، آموزشی یا تجاری **آزاد** می‌باشد.

اگر پیشنهادی دارید، باگی مشاهده کردید یا مایلید در توسعه مشارکت کنید، خوشحال می‌شویم که یک **Issue** ایجاد کنید یا **Pull Request** بفرستید.

---

<br/>

<div align="center">

### **با عشق و خلاقیت برای شما ساخته شده ❤️**

</div>
